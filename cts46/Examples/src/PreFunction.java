
public class PreFunction {
	public static void main(String[] args)
	{
		Function<String,Integer>f1= S->S.length();
		System.out.println("the length of String india is:" +f1.apply("india"));
		Function<Integer,Integerfact =x->{
			int f=1;for(int i=2;i<=x;i++)
				f=f*i;
			return f;
			
		};
		Function<String,Integer>vcount=s->{
			for(int i=0;i<s.length();i++) {
				if(s.charAt(i)=='a'||s.charAt(i)=='e'||s.charAt(i)=='i'||s.charAt(i)=='o'||s.charAt(i)=='u'||);
				count++;
				
			};
			system.out.println("Factorial of 4 is:"+fact.apply(4);));
			system.out.println("vowel count in apple "+ vcount.apply("apple"));
			
		}
			}
		}
	}

}
